<UserControl x:Class="AudioVisualizer1.MVVM.View.VisualizerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:AudioVisualizer1.MVVM.ViewModel"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance viewModel:VisualizerViewModel}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <CheckBox
            Grid.Row="0" Margin="0,4,0,0"
            Content="Send to GameSense" Foreground="White"
            IsChecked="{Binding SendToGameSenseChecked}"
            IsEnabled="{Binding GameSenseAvailable}" />
        <ContentPresenter Grid.Row="1" Content="{Binding SpectrumBarControl}" />
    </Grid>
</UserControl>